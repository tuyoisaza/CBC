{
  "name": "cbc-monorepo",
  "version": "1.1.0",
  "private": true,
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "postinstall": "pnpm --filter @cbc/db build",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "dependencies": {
    "turbo": "^2.0.12"
  }
}
